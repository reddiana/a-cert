본 문서는 분산 배치 엔진(BatchService)의 경량 분산 코디네이션 엔진(`batch-clustering-engine`)이 3. Architecture Driver에서 정의된 품질 속성 시나리오(QAS-01 ~ QAS-09)와 4. Top Level Design의 아키텍처 설계 의사결정(DD-01 ~ DD-07), 그리고 5.3.2 Scenario View의 안전성 불변식을 충족하는지 객관적으로 입증하기 위한 검증 전략을 정의합니다. 상세 테스트 케이스는 7.2절을 참조합니다.

### 7.1.1 검증 개요 및 목표

본 아키텍처 검증의 핵심 목표는 외부 분산 미들웨어(Zookeeper, Redis, Hazelcast 등) 없이 표준 Java 런타임 위에 In-Process 라이브러리(DD-06)로 구현된 다음 메커니즘이 비기능 요구사항(일관성, 성능, 신뢰성, 가용성, 효율성, 이식성, 내구성)을 충족하는지 사전에 확인하는 것입니다.

| 설계 결정 | 검증 대상 구현 | 주요 검증 관점 |
| :--- | :--- | :--- |
| DD-01 Socket P2P Raft | `RaftNode`, `SocketNetworkTransport` | PreVote/RequestVote 선출, 로그 최신성 투표, Check-Quorum 강등, 멤버십 자가 치유, Split-Brain 방지 |
| DD-02 Raft FSM 분산 락 | `LockStateMachine`, `DistributedLockExecutor` | 상호 배제, FIFO 대기열 승격, lease 갱신·만료, fenceToken 펜싱 |
| DD-03 Raft FSM 분산 큐 | `QueueStateMachine`, `DistributedQueueService` | 커밋 후 인출, 중복·유실 0건, in-flight lease·ACK, Group Commit 성능 |
| DD-04 분산 저널링 | `JournalStateMachine`, `TaskJournalService`, `RecoveryCoordinator` | 고립 작업 판정, Execution Target 상태 기반 재적재·인수·완료, 구 보고 거부 |
| DD-05 로컬 WAL + Write-Behind | `RaftLogManager`, `WriteBehindSynchronizer`, `JdbcDatabaseStorage` | fsync WAL 복원, DB 장애 중 무중단, DB 체크포인트 기반 멱등 Reconciliation |
| DD-06 Embedded Library | `BatchClusteringAutoConfiguration`, `BatchClusterProperties` | 외부 프로세스 없는 부트스트랩, 자원 점유, 설정 누락 시 Fail-fast |
| DD-07 StatefulSet | (크래시/재기동 시뮬레이션) | 동일 식별자·WAL 디렉터리로 재기동 후 재합류 |

모든 검증은 개발자가 별도의 외부 인프라 서버를 구축하지 않고도 로컬 개발 환경에서 즉시 실행하고 판정할 수 있도록 표준 테스트 프레임워크(JUnit 5, AssertJ, Awaitility)와 Gradle Wrapper를 기반으로 구성합니다.

#### 검증 원칙
1. **실제 합의 경로 검증**: 가상 클러스터의 각 노드는 WAL 디렉터리, Multi-FSM, 합의 엔진을 **독립적으로** 보유하며, 상태를 공유하거나 리더를 강제 지정하지 않습니다. 모든 상태 변경은 과반수 커밋 후 FSM apply 결과로만 확인합니다.
2. **명세 임계값 준수**: 테스트 단언문은 3.2 QAS의 응답 측정 기준(시간, 오류 건수)을 완화 없이 그대로 사용합니다.
3. **시나리오 추적성**: 각 테스트 케이스는 5.3.2 Scenario의 측정 구간(시작 → 종료 지점)과 1:1로 대응합니다 (7.2.0 추적표).
4. **결함 회귀 방지**: 아키텍처 설계 검토에서 식별된 결함은 별도 회귀 테스트(`RaftSafetyTest`)로 고정합니다.

### 7.1.2 단계별 검증 전략

검증 범위와 특성에 따라 테스트를 3단계로 분류하여 수행합니다.

![[ArchitectureSpecification/7. Architecture Implementation and Verification/7.1.2 Verification Stages.svg]]

1. **1단계: 동시성 및 구성 요소 검증 (Concurrency & Component Testing)**
   - 멀티스레드 환경(`ExecutorService`, `CountDownLatch`)에서 다수의 스레드가 3개 노드의 분산 락·분산 큐 API에 동시에 접근할 때, 팔로워 노드 요청의 리더 전달, FIFO 락 승격, 커밋 후 인출이 유지되는지 검증합니다.
   - 임계 구역 판정 시간(평균 10ms 이하), 100건 전체 처리(1초 이내), 개별 인출 지연(평균 20ms·최대 50ms 이하), 200건 전체 인출(0.5초 이내)을 계측합니다.
   - Write-Behind 대상 JDBC 저장소는 인메모리 H2로 트랜잭션 원자성과 멱등성을 단위 검증합니다.

2. **2단계: 가상 클러스터 기반 장애 시뮬레이션 검증 (Virtual Cluster & Fault Simulation Testing)**
   - 단일 JVM 프로세스 내에서 3개의 가상 노드를 구동하고, 프로세스 크래시(`crash`), 동일 WAL로 재기동(`restart`), 네트워크 격리(`isolateNode`)를 주입하여 리더 선출, 페일오버, 로그 절단·재수렴 흐름을 재현합니다.
   - DB 장애 주입 저장소(`MockDatabaseProxy`)로 메타 DB 단절 중에도 합의·락·큐가 중단 없이 동작하고, 복구 후 DB 체크포인트 기준으로 동기화되는지 확인합니다.
   - Execution Target 상태 조회(`ExecutionStatusProvider`)를 Mock으로 대체하여 고립 작업의 재적재·추적 인수·완료 판정을 검증합니다.

3. **3단계: 시스템 자원 및 배포 독립성 검증 (Resource & Packaging Testing)**
   - Java 표준 `MemoryMXBean` 및 `GarbageCollectorMXBean`으로 엔진 기동 전후의 Heap 증가량(50MB 이하)과 테스트 구간 GC 시간(1초 이내)을 측정합니다.
   - 가상 네트워크가 아닌 실제 `SocketNetworkTransport`(공유 토큰 핸드셰이크, 바이너리 프레임)와 로컬 WAL만으로 3개 노드가 부트스트랩되어 소켓 경로로 복제·커밋되는지 확인합니다.
   - Spring Boot 자동 구성(`BatchClusteringAutoConfiguration`)으로 수요처 애플리케이션에 주입되는 경로를 검증합니다.

### 7.1.3 테스트 Mock 환경 및 공통 유틸리티 구성

로컬 환경에서 분산 네트워크와 장애 상황을 재현하기 위해 테스트 헬퍼 클래스(`src/test/java/com/batchservice/cluster/`)를 활용합니다. `VirtualCluster`의 구성은 다음과 같으며, 헬퍼 클래스 전체는 아래 표와 같습니다.

![[ArchitectureSpecification/7. Architecture Implementation and Verification/7.1.3 Test Environment.svg]]

| 헬퍼                                    | 역할                                                                                                                                                                                                                       | 주요 API                                                                                                                                                                                                                                                                                                                          |
| :------------------------------------ | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `mock.VirtualCluster`                 | N개(기본 3개, TC-QAS-07은 1개) 가상 노드의 생명주기 관리. 노드별 독립 WAL 디렉터리(`@TempDir/node-N`), Multi-FSM, 합의 엔진, Write-Behind, 복구 조정기를 구성하고, 노드가 공유하는 `VirtualNetworkRouter`·`MockDatabaseProxy`·`ExecutionStatusProvider`를 보유. 리더는 선출로 결정 | `start()`, `crash(nodeId)`, `restart(nodeId)`, `setClockOffset(nodeId, offsetMs)`, `shutdown()`, `getLeaderNode()`, `getActiveLeaderCount()`, `runningNodes()`, `getNetworkRouter()`, `getLockExecutor(nodeId)`, `getQueueService(nodeId)`, `getJournalService(nodeId)`, `getWriteBehindSynchronizer()`, `getRecoveryActions()` |
| `mock.VirtualNetworkRouter`           | `NetworkTransport` 구현. 노드 간 메시지를 수신 노드별 단일 스레드로 순서대로 전달(실제 TCP 영속 연결과 동일)하고, 특정 노드의 송수신을 차단하여 네트워크 분할·하트비트 누락을 재현                                                                                                        | `isolateNode(nodeId)`, `reconnectNode(nodeId)`, `unblockAll()`, `shutdown()`                                                                                                                                                                                                                                                    |
| `mock.MockDatabaseProxy`              | `DatabaseStorage` 구현 인메모리 메타 DB. 연결 차단 시 `SQLException` 발생, 이력 반영·체크포인트 갱신을 원자적·log_index 멱등으로 처리                                                                                                                        | `disconnect()`, `reconnect()`, `readCheckpoint(clusterId)`, `writeBatch(...)`, `countRecords()`                                                                                                                                                                                                                                 |
| `ExecutionStatusProvider` (람다)        | Execution Target(K8s Job 등)의 실행 상태 조회 Mock                                                                                                                                                                               | `(executionId, jobId) -> ExecutionStatus`                                                                                                                                                                                                                                                                                       |
| `bootstrap.StandaloneBootstrapEngine` | 로컬 빈 포트에 실제 `SocketNetworkTransport` + `RaftLogManager` + `RaftNode` N개를 기동 (TC-QAS-08)                                                                                                                                  | `bootstrap()`, `isClusterReady()`, `getLeader()`, `getRequiredExternalProcessCount()`, `stop()`                                                                                                                                                                                                                                 |
| `mock.DagStateManager`                | DAG 선후행 상태 전이(READY → RUNNING) Mock 객체                                                                                                                                                                                   | `transitionToRunning(taskId)`                                                                                                                                                                                                                                                                                                   |

#### 장애 주입 의미
- **`crash(nodeId)`**: 스레드를 즉시 정지하고 종료 시점의 commitIndex 영속화를 생략합니다(평시 commitIndex는 1초 주기로 영속화). WAL 파일은 디스크에 유지되어 Pod 비정상 종료를 재현합니다.
- **`restart(nodeId)`**: 동일 WAL 디렉터리로 새 인스턴스를 기동하여 StatefulSet의 동일 식별자·PVC 재마운트(DD-07)를 재현합니다.
- **`isolateNode(nodeId)`**: 해당 노드가 보내거나 받는 모든 메시지를 드롭하여 1:2 네트워크 분할을 재현합니다. `reconnectNode(nodeId)`로 분할을 해소합니다.
- **`disconnect()` / `reconnect()`** (`MockDatabaseProxy`): 메타 DB 연결을 차단하여 체크포인트 조회·이력 반영에서 `SQLException`을 발생시키고, 연결 복구 후 Write-Behind가 DB 체크포인트부터 재동기화하는지 검증합니다.
- **`setClockOffset(nodeId, offsetMs)`**: 해당 노드의 시스템 시계에 오차를 주입하여, 리더 교체 시 노드 간 시계 차이가 lease 만료 판정에 영향을 주지 않는지 검증합니다 (`RaftSafetyTest`).

### 7.1.4 테스트 실행 환경 및 검증 절차 가이드

#### 1. 개발 및 실행 환경 요구사항
- **Java Runtime**: JDK 17 (Gradle toolchain `languageVersion = 17`). `JAVA_HOME` 또는 `PATH`에 JDK 17이 설정되어 있어야 합니다.
- **빌드 도구**: Gradle Wrapper 9.2.1 (프로젝트 포함, 별도 설치 불필요)
- **테스트 라이브러리 의존성** (Spring Boot 3.2.4 BOM 관리):
  - `org.springframework.boot:spring-boot-starter-test` (JUnit Jupiter 5.10.x, AssertJ 3.24.x 포함)
  - `org.awaitility:awaitility:4.2.1`
  - `org.springframework.boot:spring-boot-starter-jdbc`, `com.h2database:h2` (JDBC 저장소·자동 구성 테스트)
  - `org.springframework.boot:spring-boot-actuator` (HealthIndicator 자동 구성)

#### 2. 테스트 실행 방법
- **IDE 환경 (IntelliJ IDEA / Eclipse / VSCode)**:
  - `src/test/java` 경로의 `ArchitectureVerificationTest.java`, `RaftSafetyTest.java` 또는 개별 테스트 메소드 우클릭 $\rightarrow$ `Run Test` 실행.
- **Gradle CLI 환경** (Windows는 `gradlew.bat`):
  ```bash
  # 전체 테스트 일괄 실행 (이전 빌드 산출물 제거 포함)
  ./gradlew clean test

  # 아키텍처 검증 테스트 일괄 실행
  ./gradlew test --tests "com.batchservice.cluster.ArchitectureVerificationTest"

  # 특정 품질 속성 시나리오(예: QAS-01 일관성) 개별 실행
  ./gradlew test --tests "com.batchservice.cluster.ArchitectureVerificationTest.testQAS01_DagConcurrencyConsistency"

  # 안전성 회귀 테스트 실행
  ./gradlew test --tests "com.batchservice.cluster.RaftSafetyTest"
  ```

#### 3. 결과 확인 및 판정 기준
- **결과 리포트**: `build/reports/tests/test/index.html` (테스트별 성공 여부, 소요 시간, 표준 출력)
- **계측 로그**: 각 테스트는 `TC-QAS-0x:` 접두어로 처리 시간, 획득·인출 건수, 선출된 리더 ID, 복구 조치, 자원 사용량을 출력합니다 (`build/test-results/test/TEST-*.xml`의 `system-out`에서도 확인 가능).
- **판정 기준**: 모든 테스트가 `BUILD SUCCESSFUL`로 종료되고, 각 테스트 케이스의 정량 단언문(시간 지연, 오류·중복·유실 0건 등)을 만족하면 아키텍처 적합성 검증이 완료된 것으로 판정합니다.
- **검증 결과**: 실행 정보, TC-QAS-01 ~ 09별 측정값과 목표 대비 결과, 안전성 회귀 테스트 결과는 **7.2.12 검증 결과 요약**에 정리되어 있습니다. (2026-09-14 Linux 환경 실행 기준 17건 전체 통과)

### 7.1.5 검증 범위의 한계 및 보완 계획

| 구분 | 로컬 검증의 한계 | 보완 계획 |
| :--- | :--- | :--- |
| 네트워크 | 가상 라우터는 완전 차단만 재현하며, 실제 지연·패킷 손실·부분 단절은 재현하지 않음 | K8s 스테이징 환경에서 NetworkPolicy·장애 주입 도구로 부분 단절 검증 |
| 디스크 내구성 | 테스트 WAL은 JUnit `@TempDir`(Linux `/tmp`, tmpfs)에 기록되어 디스크 fsync 지연이 측정값에 반영되지 않으며, PVC(블록 스토리지)의 fsync 지연과 다름 | 5.4 PVC 환경에서 TC-QAS-01/02 성능 재측정 |
| 자원 점유 (TC-QAS-07) | 단일 JVM에서 GC 시점에 따라 Heap 측정 편차가 큼 | 컨테이너 메트릭(Actuator/Prometheus)으로 노드당 점유 장기 관측 |
| 로그 압축 | Snapshot / InstallSnapshot / 로그 압축은 미구현으로 검증 대상에서 제외 (압축 하한선 관리만 구현) | 구현 후 장기 운영·재합류 시나리오 테스트 추가 |
| 전송 보안 | 공유 토큰 핸드셰이크만 검증하며 전송 구간 암호화(mTLS)는 검증하지 않음 | 서비스 메시(mTLS) 적용 환경에서 연결 검증 |
| Execution Target | 실행 상태 조회는 Mock 응답으로 검증 | 수요처 `ExecutionStatusProvider`(K8s Job API) 구현 후 통합 테스트 |
