본 절은 1. Project Overview, 2. System Overview, 3. Architectural Driver에서 정의된 기능 요구사항(FR), 시스템 기능(SF), 비기능 요구사항(NR), 품질 속성 시나리오(QAS), 제약 조건(CR), 비즈니스 목표(BG)가 수립된 아키텍처에서 어떤 메커니즘으로 충족되는지를 설계 관점에서 분석합니다. 각 요구사항의 충족 여부는 설계 근거(5.3.2 공통 런타임 규약의 시간 예산, 5.4 용량 산정)로 판단하며, 이를 실측으로 확인하는 테스트 케이스는 7. Architecture Implementation and Verification에서 다룹니다.

---

### 6.3.1 기능 요구사항(FR) 및 시스템 기능(SF) 관점 분석

4가지 기능 요구사항과 5가지 시스템 기능이 아키텍처에 구현된 방식과 설계 충족 근거는 다음과 같습니다.

| Req ID | 시스템 기능 | 요구사항 정의 | 구현 아키텍처 메커니즘 | 설계 충족성 | 검증 TC |
| :----: | :------------: | :------------------------ | :--- | :---------: | :---: |
| FR-01 | SF-02 | 중복 실행 방지 분산 락 | `LockStateMachine`이 Raft 로그로 커밋된 락 명령을 모든 노드에서 동일 순서로 apply하는 결정론적 FSM(소유자·fenceToken·lease·FIFO 대기열)으로 상호 배제를 보장함. `DistributedLockExecutor`는 팔로워 노드 요청을 리더로 전달하고, 임계 구역 수행 중 lease를 갱신하며 fenceToken으로 해제하므로, lease 만료 후 구 소유자의 뒤늦은 해제·갱신도 거부됨. | 충족 | `TC-QAS-01` |
| FR-02 | SF-03 | 작업 누락 방지 분산 큐 | `QueueStateMachine`은 커밋된 `QUEUE_DEQUEUE` 엔트리를 apply하는 시점에 인출 대상을 결정하여 단일 작업을 오직 하나의 워커에만 할당함. 인출된 작업은 ACK 전까지 in-flight lease로 보관되어 워커 장애·lease 만료 시 재적재됨(At-least-once). | 충족 | `TC-QAS-02` |
| FR-03 | SF-04 | 저널 복구를 통한 무유실 보장 | `JournalStateMachine`이 태스크 실행 상태(`PENDING`, `RUNNING`, `COMPLETED`, `FAILED`)와 추적 노드·leaseToken을 기록하며, 모든 명령은 `RaftLogManager`의 fsync WAL과 Raft Log 복제로 영속화됨. 추적 노드 크래시 시 신규 리더의 `RecoveryCoordinator`가 멤버십과 Execution Target 상태를 확인하여 재적재·추적 인수·완료를 로그로 커밋하며, 구 추적자의 뒤늦은 보고는 펜싱으로 거부함. | 충족 | `TC-QAS-06` |
| FR-04 | SF-01<br>SF-05 | 클러스터 멤버십 자가 치유 및 자동 리더 선출 | `RaftNode`가 500ms 주기 Heartbeat를 전송하고 팔로워별 응답 수신 시각으로 2초 무응답 장애를 판정하여 `MEMBER_STATUS`를 과반수 커밋함. 리더 장애 시 Election Timeout(1,500~2,000ms 난수) 경과 후 PreVote·RequestVote(로그 최신성 검사)로 선출하고, 신규 리더가 `NO_OP`을 커밋한 시점부터 서비스를 재개함. | 충족 | `TC-QAS-04`<br>`TC-QAS-05` |

---

### 6.3.2 비기능 요구사항(NR) 및 품질 속성 시나리오(QAS) 관점 분석

품질 속성 6대 분류(일관성, 성능, 가용성, 신뢰성, 효율성, 이식성/운용성) 및 DB 장애 내구성 요구사항에 대해, 목표(3.2 응답 측정)와 이를 충족하는 설계 근거는 다음과 같습니다.

| 품질 속성 | QAS / NR | 목표 (3.2 응답 측정) | 핵심 설계 메커니즘 | 검증 TC |
| :--- | :--- | :--- | :--- | :---: |
| 일관성 / 정합성 | `QAS-01`, `QAS-03`<br>`NR-02` | 동시 100건 상태 왜곡 0건, 임계 구역 평균 10ms 이하·전체 1초 이내<br>분할 시 활성 리더 1개 이하 | Lock FSM 상호 배제, Quorum 커밋, Check-Quorum 강등 | `TC-QAS-01`<br>`TC-QAS-03` |
| 성능 / 저지연 | `QAS-02`<br>`NR-02`, `NR-06` | 개별 인출 평균 20ms·최대 50ms 이하, 200건 0.5초 이내 | In-Process FSM, Group Commit(≤2ms 또는 64건) | `TC-QAS-02` |
| 가용성 / 복원력 | `QAS-04`, `QAS-05`<br>`NR-04` | 팔로워 제외 3초 이내<br>신규 리더 정상화 3초 이내 | 감지 2초 + 멤버십 커밋 1초<br>감지 2초 + 선출·`NO_OP` 커밋 1초 | `TC-QAS-04`<br>`TC-QAS-05` |
| 신뢰성 / 무유실 | `QAS-06`<br>`FR-03`, `NR-05` | 유실 0건, 고립 작업 복구 10초 이내 | fsync WAL, `RecoveryCoordinator`, leaseToken 펜싱 | `TC-QAS-06` |
| 자원 효율성 | `QAS-07`<br>`NR-06` | 추가 Heap 50MB 이하, GC STW 1초 이내 | In-Process 임베디드 라이브러리, FSM 약 41MB 산정<br>※ 장기 운영 전 로그 압축 필요 | `TC-QAS-07` |
| 이식성 / 운용성 | `QAS-08`<br>`NR-01`, `NR-03`, `NR-07`, `NR-08` | 미들웨어 설치 0분, 신규 노드 가입 2분 이내 | 외부 미들웨어 0개, StatefulSet·Headless Service 자동 구성 | `TC-QAS-08` |
| DB 장애 내구성 | `QAS-09`<br>`NR-09` | DB 장애 중 중단 0초, 복구 후 30초 이내 동기화, 누락 0건 | DB 미경유 합의 경로, WAL 기반 Write-Behind 복원 | `TC-QAS-09` |

#### 1. 일관성 (Consistency) - `QAS-01`, `QAS-03`, `NR-02`
* **아키텍처 대응**: 결정론적 Lock/Queue FSM, 커밋 후 apply 결과만 반환하는 선반영 금지 원칙, Raft Quorum(`(N/2)+1`) 커밋 규칙, 로그 최신성 투표 검사, Check-Quorum 기반 리더 자진 강등을 적용함.
* **설계 충족 근거**:
  - 어느 노드로 유입된 요청이든 리더의 단일 Raft 로그 순서로 직렬화되어 `LockStateMachine`에 apply되므로, 동시 100건의 상태 전이 경합에서도 임계 구역에는 한 번에 하나의 요청만 진입하고 후행 작업 발화는 1회로 제한됨. 임계 구역은 메타 DB가 아닌 로컬 Multi-FSM 조회로 판정하므로(5.3.2 Scenario 1) 개별 판정 지연을 10ms 이하로 유지할 수 있음.
  - 1:2 네트워크 분할 시 소수파 리더는 과반수 응답을 받지 못해 커밋·인출이 불가능하고 Election Timeout 이내에 Check-Quorum으로 강등되며, 다수파만 신규 리더를 선출할 수 있으므로 활성 리더는 항상 1개 이하임. 복구 시 소수파의 미커밋 엔트리는 로그 불일치 절단으로 제거되어 로그가 재수렴함 (5.3.2 Scenario 5).
* **검증 연계**: `TC-QAS-01`(동시 100건 락 경합), `TC-QAS-03`(리더 소수파 격리)

#### 2. 성능 (Performance) - `QAS-02`, `NR-02`, `NR-06`
* **아키텍처 대응**: RDBMS 트랜잭션 락(`FOR UPDATE`) 대신 In-Process 인메모리 FSM을 사용하고, 동시 명령을 Group Commit(1회 `fsync`·일괄 AppendEntries)으로 처리하며, 노드 쌍별 영속 TCP 연결로 복제 지연을 최소화함.
* **설계 충족 근거**:
  - 인출 경로에 DB 왕복이 없고, 동시 인출 요청은 Group Commit 윈도(최대 2ms 또는 64건)로 묶여 요청 수만큼 `fsync`·복제가 반복되지 않으므로, 개별 인출 지연 평균 20ms·최대 50ms 이하와 200건 전체 0.5초 이내 목표를 만족하도록 설계함 (5.3.2 공통 런타임 규약).
* **검증 연계**: `TC-QAS-02`(3노드 12개 워커의 200건 동시 인출)

#### 3. 가용성 (Availability) - `QAS-04`, `QAS-05`, `NR-04`
* **아키텍처 대응**: 500ms Heartbeat, 팔로워 응답 기반 2초 장애 판정과 `MEMBER_STATUS` 커밋, Election Timeout 1,500~2,000ms 난수, PreVote로 고립 노드의 term 인플레이션 방지, 신규 리더 `NO_OP` 커밋을 적용함.
* **설계 충족 근거**:
  - **팔로워 장애 (`QAS-04`)**: 마지막 AppendResponse 후 2,000ms(Heartbeat 4회 누락)에 DEAD로 판정하고, `MEMBER_STATUS(DEAD)`를 과반수 커밋·동기화하는 1초 예산을 더해 총 3초 이내에 활성 멤버십에서 제외함. 판정 임계치 2초는 GC STW(최대 1초, `QAS-07`)보다 길어 정상 노드 오탐을 배제함.
  - **리더 장애 (`QAS-05`)**: Election Timeout 상한 2,000ms로 감지 단계 2초 이내, 선출 RPC 타임아웃 200ms와 Split Vote 재시도 backoff 150~300ms로 선출·`NO_OP` 커밋 1초 이내를 확보하여 총 3초 이내에 서비스를 재개함 (5.3.2 Scenario 2·3).
* **검증 연계**: `TC-QAS-04`(Follower Crash), `TC-QAS-05`(Leader Crash)

#### 4. 신뢰성 (Reliability) - `QAS-06`, `FR-03`, `NR-05`
* **아키텍처 대응**: `fsync` 기반 로컬 WAL 영속화, Raft Log 복제, `RecoveryCoordinator`의 추적 노드 생존·Execution Target 상태 기반 복구, leaseToken 펜싱을 연계함.
* **설계 충족 근거**:
  - 작업 상태는 과반수 노드의 WAL에 커밋된 후에만 확정되므로 단일 노드 크래시로 저널이 유실되지 않음. 신규 리더 승격 후 `RecoveryCoordinator`가 DEAD 노드가 추적하던 작업만 골라 Target 상태에 따라 `REQUEUED`(NOT_FOUND·FAILED·UNKNOWN), `ADOPTED`(RUNNING, 재실행 없음), `COMPLETED`로 처리하고, 생존 노드가 추적 중인 작업은 조치하지 않아 중복 실행을 방지함. 승격(3초) 후 10초 이내 복구 예산으로 설계함 (5.3.2 Scenario 2).
  - 재기동 노드는 WAL Replay로 FSM을 복원한 뒤 로그를 따라잡아 멤버십에 재합류함 (5.3.2 Scenario 3).
* **검증 연계**: `TC-QAS-06`(추적 리더 크래시 후 고립 작업 복구), `RaftSafetyTest.restartedNodeRecoversFromWalAndRejoins`

#### 5. 효율성 (Efficiency) - `QAS-07`, `NR-06`
* **아키텍처 대응**: 별도 사이드카 프로세스나 외부 JVM 데몬 없이 기존 애플리케이션 JVM 힙 내에서 구동되는 In-Process 임베디드 라이브러리 구조를 채택하고, JPA 등 불필요한 의존성 없이 JDBC만 선택적으로 사용함.
* **설계 충족 근거**:
  - In-Memory Multi-FSM 상태를 Lock FSM(1MB) + Queue FSM(20MB) + Journal FSM(20MB) = 약 41MB로 산정하여 추가 Heap 50MB 이하 요건 내에 두고, 프로세스 간 통신(IPC)을 제거함 (5.4 용량 산정).
  - 단, 로그 압축(Snapshot/Compaction) 미구현으로 운영 부하에서는 인메모리 로그 사본이 일 약 40~70MB씩 누적되므로, 장기 무중단 운영 전 로그 압축 구현이 필요함 (5.4.2-4, `RISK-01`).
* **검증 연계**: `TC-QAS-07`(1노드 기동·1,000건 적재 후 Heap·GC 측정), 운영 환경 K8s 컨테이너 메트릭 (7.1.5)

#### 6. 이식성 및 운용성 (Portability & Operability) - `QAS-08`, `NR-01`, `NR-03`, `NR-07`, `NR-08`
* **아키텍처 대응**: Zookeeper, Redis 등 외부 분산 미들웨어 의존성을 전면 배제하고, Spring Boot Auto-Configuration 표준 패키징, JDK 17 표준 소켓, 5.4 매니페스트 환경변수(`RAFT_NODE_ID`, `RAFT_PEERS`, `RAFT_AUTH_TOKEN`, `WAL_DATA_DIR`) 기반 자동 구성을 적용함. 피어 설정 누락 시 단일 노드 기동을 거부(Fail-fast)하여 Split-Brain을 방지함.
* **설계 충족 근거**:
  - 클러스터 구성에 필요한 사전 미들웨어가 없으므로 미들웨어 설치 시간은 0분이며, StatefulSet `Parallel` 기동과 Headless Service(`publishNotReadyAddresses`)로 피어가 동시에 서로를 발견한 뒤 리더 선출(수 초)을 거쳐 Readiness에 도달하므로 신규 노드 배포 후 2분 이내 가입 목표를 만족함 (5.4.2).
* **검증 연계**: `TC-QAS-08`(실제 TCP 소켓 3노드 부트스트랩)

#### 7. DB 장애 내구성 (Database Outage Resilience) - `QAS-09`, `NR-09`
* **아키텍처 대응**: 리더 선출(DD-01), 분산 락(DD-02), 분산 큐(DD-03), 저널링(DD-04)이 메타 DB 없이 In-Memory FSM 및 로컬 WAL만으로 완결되도록 설계하고, 리더의 `WriteBehindSynchronizer`가 DB 동기화 체크포인트를 기준으로 커밋 구간을 반영(이력·체크포인트 단일 트랜잭션, log_index 멱등)하는 비동기 Write-Behind 복원 파이프라인을 구축함.
* **설계 충족 근거**:
  - 락·큐·저널 요청의 처리 경로(WAL `fsync` → Quorum 복제 → 커밋 → apply)에 DB I/O가 없고, Readiness Probe에 DB Health를 포함하지 않으므로 DB 장애 중에도 처리 중단 시간은 0초임.
  - DB 장애 중에는 WAL 압축 하한선이 DB 체크포인트에 고정되어 미반영 구간이 보존되며, 재시도 backoff 상한(10초)으로 복구를 감지한 뒤 체크포인트부터 500건 단위로 재생하므로 30초 이내에 누락·중복 없이 동기화함 (5.3.2 Scenario 4).
* **검증 연계**: `TC-QAS-09`(DB 단절 중 락·큐 수행 후 복구 동기화)

---

### 6.3.3 제약 조건(CR) 및 비즈니스 목표(BG) 달성 분석

|   제약 / 목표 ID   | 내용                            | 아키텍처 대응 및 달성 분석                                                                                                                                                                                                    |     달성 여부     |
| :------------: | :---------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :-----------: |
| CR-01<br>BG-01 | 유료 라이선스 모듈 배제 및 TCO 절감    | 상용 Hazelcast 의존성을 완전 배제하고 자체 Raft 합의 및 Multi-FSM 코어를 오픈소스/표준 JDK로 구현함. 3노드 클러스터 기준 연간 약 $15,000(약 2,000만 원), 전사 확대 시 수억 원의 라이선스 고정비를 절감함.                                                                          |     완전 달성     |
| CR-02<br>BG-03 | 기존 K8s 환경 유지 및 재사용 배포 용이성 | 외부 미들웨어 클러스터(ZooKeeper/Redis) 신규 구축 요구를 제거하고, Spring Boot JAR 의존성 추가와 StatefulSet(`Parallel` 기동, PVC)·Headless Service(`publishNotReadyAddresses`)·`RAFT_AUTH_TOKEN` Secret 구성만으로 배포를 완료하도록 단순화함 (5.4.2 표준 매니페스트). |     완전 달성     |
|     BG-02      | 분산 코디네이션 기술 독립성 및 사내 자산화  | 특정 벤더 종속 없이 Raft P2P 합의, Multi-FSM 동시성 제어, 로컬 WAL 복구 기술을 자체 자산화하여 향후 사내 다양한 분산 서비스로 확장 가능한 기반을 마련함. 운영 성숙도를 위해 로그 압축과 전송 구간 암호화는 후속 과제로 관리함.                                                                       | 달성 (후속 과제 관리) |
